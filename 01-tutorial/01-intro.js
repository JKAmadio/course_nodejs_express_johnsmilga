/*
 *
 * 01 - Intro
 *
 * Running file with node CLI
 * Save a file and then run node <filename>.js on the terminal
 *
*/

const amount = 15

// we can use quotes
if (amount < 10)
    console.log('small number');
else
    console.log('big number');

// we can use string template template
console.log(`the amount is ${amount}`)
